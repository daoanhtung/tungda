using Enum = MyWebsite.Utils.Enum;

namespace MyWebsite.Service
{
    public class ServiceHelper
    {
        //public static List<BasicData> CRS610MI_GetBasicData(BasicData request)
        //{
        //    var response = M3Service.GetBasicData(request).Select(ModelMapping.Map<BasicData>);
        //    return response.ToList();
        //}
    }
}
